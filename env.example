# Copy this file to .env for local dev, or set the same key as a Netlify
# environment variable (Site settings -> Environment variables) for production.
# Use wss:// (not ws://) once your backend is deployed behind HTTPS.
VITE_WS_URL=ws://localhost:8765
